apiVersion: kubeproxy.config.k8s.io/v1alpha1
kind: KubeProxyConfiguration

bindAddress: 0.0.0.0
clusterCIDR: "{{ rede_cidr_pods }}"

mode: ipvs
ipvs:
  strictARP: true

nodePortAddresses:
  - "{{ rede_cidr_hosts }}"

clientConnection:
  kubeconfig: /etc/kubernetes/kube-proxy.kubeconfig