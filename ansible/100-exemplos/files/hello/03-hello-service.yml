apiVersion: v1
kind: Service
metadata:
  name: hello-ingress
  namespace: exemplos
spec:
  selector:
    app: hello-ingress
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
