apiVersion: v1
kind: Service
metadata:
  name: contador
  namespace: exemplos
  labels:
    app: contador-pvc
spec:
  selector:
    app: contador
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
