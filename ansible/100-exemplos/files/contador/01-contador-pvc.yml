apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: contador-pvc
  namespace: exemplos
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Mi
  storageClassName: servidor-nfs
